{
  "salesforce_accounts": {
    "required": ["Id", "Name", "LastModifiedDate"],
    "types": {
      "Id": "string",
      "Name": "string",
      "LastModifiedDate": "timestamp"
    }
  },
  "hubspot_contacts": {
    "required": ["id", "properties", "updatedAt"],
    "types": {
      "id": "string",
      "properties": "object",
      "updatedAt": "timestamp"
    }
  },
  "stripe_charges": {
    "required": ["id", "amount", "currency", "created"],
    "types": {
      "id": "string",
      "amount": "integer",
      "currency": "string",
      "created": "integer"
    }
  },
  "zendesk_tickets": {
    "required": ["id", "subject", "status", "updated_at"],
    "types": {
      "id": "integer",
      "subject": "string",
      "status": "string",
      "updated_at": "timestamp"
    }
  },
  "jira_issues": {
    "required": ["id", "key", "status", "updated"],
    "types": {
      "id": "string",
      "key": "string",
      "status": "string",
      "updated": "timestamp"
    }
  },
  "shopify_orders": {
    "required": ["id", "customer_id", "total_price", "updated_at"],
    "types": {
      "id": "integer",
      "customer_id": "string",
      "total_price": "string",
      "updated_at": "timestamp"
    }
  },
  "netsuite_invoices": {
    "required": ["internalId", "customer_id", "amountDue", "lastModifiedDate"],
    "types": {
      "internalId": "string",
      "customer_id": "string",
      "amountDue": "integer",
      "lastModifiedDate": "timestamp"
    }
  },
  "google_analytics_events": {
    "required": ["eventId", "customer_id", "eventName", "eventDate"],
    "types": {
      "eventId": "string",
      "customer_id": "string",
      "eventName": "string",
      "eventDate": "timestamp"
    }
  }
}
